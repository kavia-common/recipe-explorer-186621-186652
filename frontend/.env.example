# Example environment variables for Recipe Explorer frontend
# Copy to .env and adjust as needed. Do not commit .env to version control.

# Base URL for API (if provided, app will use this)
REACT_APP_API_BASE=https://your-api.example.com

# Alternative API base (used if REACT_APP_API_BASE not set)
REACT_APP_BACKEND_URL=https://your-backend.example.com

# Optional
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_NODE_ENV=development
